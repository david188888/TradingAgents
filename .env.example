# LLM Providers (set the one you use)
OPENAI_API_KEY=
GOOGLE_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=
DASHSCOPE_CN_API_KEY=
ZHIPU_API_KEY=
ZHIPU_CN_API_KEY=
MINIMAX_API_KEY=
MINIMAX_CN_API_KEY=
OPENROUTER_API_KEY=

# FRED (Federal Reserve macro data: rates, inflation, labor, growth). Free key: https://fred.stlouisfed.org/docs/api/api_key.html
#FRED_API_KEY=

# A-share data (tushare/akshare) - required for .SS/.SZ tickers. Tushare token
# from https://tushare.pro/; akshare needs no key. Used by the A-share vendor
# path (verified snapshot + indicators) and get_stock_data.
#TUSHARE_TOKEN=
#TUSHARE_API_KEY=

# Wind AIFin Market (https://aifinmarket.wind.com.cn) - premium A-share index,
# macro EDB, risk metrics, fundamentals and document retrieval. Obtain a key
# from the developer portal; keep it out of logs and fixtures. Disabled by
# default; enable via the wind.enabled config flag.
#WIND_API_KEY=

# News search (Tavily) - enhances news/global-news analyst coverage.
# Free key: https://tavily.com/
#TAVILY_API_KEY=
# Optional round-robin key pool.  429/5xx/network health is tracked per key;
# 401/403 is reported explicitly and never bypassed by rotating credentials.
#TAVILY_API_KEYS=

# Doubao (豆包搜索 Global 版) web search - Volcengine/ByteDance search API.
# Key from: https://console.volcengine.com/ark (联网搜索控制台 - API Key 管理)
# Free tier: 500 calls/month per Volcengine account; no server-side date filter.
#DOUBAO_SEARCH_API_KEY=
#DOUBAO_SEARCH_API_KEYS=

# Bocha (博查) web search - domestic Bing-compatible search API.
# Key from: https://open.bocha.cn (paid per query; see their pricing page)
#BOCHA_API_KEY=
#BOCHA_API_KEYS=first-key,second-key

# Optional: point at a remote Ollama server. When unset, defaults to
# the local instance at http://localhost:11434/v1. Convention follows
# the broader Ollama ecosystem; both the CLI dropdown and programmatic
# client pick this up.
#OLLAMA_BASE_URL=http://your-ollama-host:11434/v1

# Optional: override DEFAULT_CONFIG without editing code.
# Any TRADINGAGENTS_* variable below, when set, replaces the matching key
# in tradingagents/default_config.py. Values are coerced to the type of
# the existing default (bool / int / str), so "true"/"3" work as expected.
# In the CLI, setting the LLM provider / models / backend URL / language
# also skips the matching interactive selection step (useful for
# OpenAI-compatible endpoints like opencode or LM Studio, and unattended runs).
#TRADINGAGENTS_LLM_PROVIDER=openai
#TRADINGAGENTS_DEEP_THINK_LLM=gpt-5.4
#TRADINGAGENTS_QUICK_THINK_LLM=gpt-5.4-mini
#TRADINGAGENTS_LLM_BACKEND_URL=
#TRADINGAGENTS_OUTPUT_LANGUAGE=English
#TRADINGAGENTS_MAX_DEBATE_ROUNDS=1
#TRADINGAGENTS_MAX_RISK_ROUNDS=1
#TRADINGAGENTS_CHECKPOINT_ENABLED=false

# Evidence gate. PASS proceeds normally; LOW_CONFIDENCE proceeds with explicit
# limitations and downstream conviction caps. Hard identity conflicts and fatal
# core-data failures remain FAIL_STOP. The default stop-on-fail switch is false.
#TRADINGAGENTS_EVIDENCE_GATE_ENABLED=true
#TRADINGAGENTS_EVIDENCE_STOP_ON_FAIL=false
#TRADINGAGENTS_NEWS_MIN_COMPANY_ITEMS=3
#TRADINGAGENTS_NEWS_MIN_MIXED_ITEMS=5
#TRADINGAGENTS_HALT_ON_MISSING_DATA=true
# Sampling temperature (lower = less run-to-run variation on models that
# honor it). Unset leaves each provider at its default. See the README
# "Reproducibility" note — no setting makes LLM output fully deterministic.
#TRADINGAGENTS_TEMPERATURE=0.0
# LLM SDK retry budget forwarded to every provider. Unset leaves each SDK at its
# own default (usually 2). Raise it to ride out bursty 429 rate-limit throttling
# on rate-limited deployments (e.g. Azure OpenAI) instead of aborting the run.
#TRADINGAGENTS_LLM_MAX_RETRIES=6
# Provider-specific reasoning/thinking depth (optional; unset = provider
# default). Setting one also skips the matching interactive prompt.
#TRADINGAGENTS_OPENAI_REASONING_EFFORT=medium
#TRADINGAGENTS_GOOGLE_THINKING_LEVEL=high
#TRADINGAGENTS_ANTHROPIC_EFFORT=high
# DeepSeek V4 thinking mode (default enabled with max effort — the fork's
# default path). While enabled, the API ignores temperature/top_p, tool_choice
# is unsupported, and the client echoes reasoning_content back on the next
# turn. Set disabled to use the non-thinking path (tool_choice + temperature).
# Effort accepts low/medium/high/xhigh/max (medium/xhigh map to high).
#TRADINGAGENTS_DEEPSEEK_THINKING=enabled
#TRADINGAGENTS_DEEPSEEK_REASONING_EFFORT=max
# Hard cap on a single model response's output tokens (optional). DeepSeek V4
# supports up to 384K output and recommends a sane cap so long JSON reports are
# not truncated mid-string. Unset leaves each provider at its own default.
#TRADINGAGENTS_LLM_MAX_TOKENS=16384
