"""Neutral runtime package (run store, checkpoint reconciliation, resume fingerprint, report publication).

Modules here are not Web/HTTP logic; ``tradingagents.web`` keeps thin facade
re-exports so existing callers and tests keep working.
"""
